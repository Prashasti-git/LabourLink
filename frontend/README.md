# LabourLink React Frontend

This Vite + React frontend recreates the original LabourLink landing page with reusable components, responsive styling, controlled forms, React Router routes, scroll effects, accessible controls, and toast feedback.

## Run locally

```bash
npm install
npm run dev
```

Create a production bundle with `npm run build`.
