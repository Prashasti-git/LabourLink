import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { SectionHead } from './SearchJobs';
const testimonials = [['RS', 'Rajesh Singh', 'Plumber · Mumbai', 'I found three consistent jobs in my first week on LabourLink. The profile verification gave clients confidence and helped me get hired faster than ever before.', 'pink'], ['AP', 'Arjun Patel', 'Contractor · Ahmedabad', 'As a contractor, finding reliable workers used to take days. With LabourLink, I hired a certified electrician in under 2 hours. The direct connection saves time and money.', 'teal'], ['PK', 'Priya Kumari', 'Painter · Lucknow', 'The location-based matching is brilliant. I get notified of jobs within 5 km of my home. My income has increased by 40% since I joined LabourLink six months ago.', 'warm']];
const faqs = [['Is LabourLink free to use?', 'LabourLink is completely free to register and create a profile. Workers can browse and apply for jobs at no cost. Hirers can post basic job listings for free, with premium options available for boosted visibility.'], ['How are workers verified on the platform?', 'Every worker undergoes a multi-step verification: government ID check, skill assessment, and reference review. Workers can also upload certificates from recognised institutions to strengthen their profiles.'], ['How does location-based matching work?', 'Workers set their preferred work radius. When a hirer posts a job, LabourLink automatically surfaces the closest verified workers matching the required skills. Both parties see a live distance indicator.'], ["What happens if a job doesn't go as planned?", 'LabourLink has a dispute resolution team available 24/7. Both workers and hirers can raise a complaint directly through the app. Our team mediates and resolves most disputes within 48 hours.'], ['Which cities are currently supported?', "We currently operate in 50+ cities across India including Delhi, Mumbai, Bangalore, Hyderabad, Ahmedabad, Lucknow, Pune, and more. We're expanding rapidly — new cities are added every month."]];
export function TrustStrip() { const items = ['Verified Identities', 'Instant Matching', 'Direct Communication', 'Skill Certified Workers', 'Location-Based Results', 'Top-Rated Professionals']; return <div className="trust-strip"><div className="trust-track">{[...items, ...items].map((text, index) => <span className="trust-pair" key={`${text}-${index}`}><span className="trust-item">{text}</span><span className="trust-sep">·</span></span>)}</div></div>; }
export function About() { const benefits = [['No Middlemen', 'Workers and hirers connect directly, reducing costs and delays.'], ['Verified Profiles', 'Every worker is reviewed and skill-verified before listing.'], ['Transparent Reviews', 'Honest ratings build long-term trust on both sides.']]; return <section className="about-section"><div className="section-inner about-grid"><div className="reveal"><span className="section-label">About the Program</span><h2 className="section-title">Why LabourLink Exists</h2><p className="section-sub">LabourLink eliminates intermediaries and inefficiencies in the labor hiring process. It empowers workers to showcase verified skills while allowing hirers to find reliable manpower quickly and confidently.</p><ul className="feature-list">{benefits.map(([title, text], index) => <li key={title}><span className="feat-icon">0{index + 1}</span><span className="feat-text"><strong>{title}</strong>{text}</span></li>)}</ul></div><div className="kpi-block reveal reveal-d2"><div className="kpi-grid">{[['12K+', 'Workers'], ['50+', 'Cities'], ['40+', 'Skill Types'], ['4.9', 'Avg Rating']].map(([num, label]) => <div className="kpi-box" key={label}><div className="kpi-num">{num}</div><div className="kpi-label">{label}</div></div>)}</div><div className="match-pill kpi-trust">Trusted by 3,400+ active hirers</div></div></div></section>; }
export function Testimonials() { return <section className="testimonials-section"><div className="section-inner"><SectionHead centered label="Success Stories" title="Trusted by Thousands" text="Real stories from workers and hirers who found success through LabourLink." /><div className="testimonials-grid">{testimonials.map(([initials, name, role, quote, tone], index) => <article className={`testi-card reveal reveal-d${index + 1}`} key={name}><div className="testi-stars">Rated 5 / 5</div><p className="testi-text">{quote}</p><div className="testi-author"><span className={`testi-avatar ${tone}`}>{initials}</span><span><span className="testi-name">{name}</span><span className="testi-role">{role}</span></span></div></article>)}</div></div></section>; }
export function Audience() { const navigate = useNavigate(); const audiences = [['Workers', 'Skilled and semi-skilled professionals can register, create profiles, and access consistent work opportunities based on their expertise and location.', 'Join as a Worker', '/workers'], ['Hirers', 'Contractors, homeowners, and organizations can post job requirements and hire verified workers directly — without agents or delays.', 'Post a Job', '/hirers']]; return <section className="who-section"><div className="section-inner"><SectionHead centered label="Who It's For" title="Built for Both Sides" text="Whether you're looking for consistent work or reliable workers, LabourLink has a place for you." /><div className="user-cards-grid">{audiences.map(([title, text, button, path], index) => <article className={`user-card reveal reveal-d${index + 1}`} key={title}><div className="user-card-icon">0{index + 1}</div><h3>{title}</h3><p>{text}</p><button className="card-cta" onClick={() => navigate(path)}>{button}</button></article>)}</div></div></section>; }
export function Steps() { const navigate = useNavigate(); const steps = [['Register', 'Create a free account as a worker or hirer in under 2 minutes.'], ['Complete Profile', 'Add skills, experience, certifications, or your job requirements.'], ['Connect', 'Apply for matching jobs or browse verified workers near you — directly.'], ['Work & Review', 'Complete tasks, get paid, and build your reputation through honest reviews.']]; return <section className="steps-section"><div className="section-inner"><SectionHead centered label="Get Started" title="Up and Running in Minutes" text="LabourLink is designed to be intuitive and accessible. Begin your journey in four simple steps." /><div className="steps-grid-wrap"><div className="steps-grid">{steps.map(([title, text], index) => <div className={`step-card reveal reveal-d${index + 1}`} key={title}><div className="step-num">{index + 1}</div><h3>{title}</h3><p>{text}</p></div>)}</div></div><div className="steps-cta reveal"><button onClick={() => navigate('/register')}>Create Your Account</button></div></div></section>; }
export function Faq() { const [open, setOpen] = useState(null); return <section className="faq-section"><div className="section-inner"><SectionHead centered label="FAQ" title="Frequently Asked Questions" text="Everything you need to know about LabourLink before getting started." /><div className="faq-list reveal">{faqs.map(([question, answer], index) => <div className={`faq-item ${open === index ? 'open' : ''}`} key={question}><button className="faq-q" onClick={() => setOpen(open === index ? null : index)} aria-expanded={open === index}>{question}<span className="faq-icon">+</span></button><div className="faq-a">{answer}</div></div>)}</div></div></section>; }
export function Newsletter({ notify }) { const [email, setEmail] = useState(''); const subscribe = (event) => { event.preventDefault(); if (!/^\S+@\S+\.\S+$/.test(email)) return notify('Notice', 'Please enter a valid email address.'); setEmail(''); notify('Success', 'Subscribed! Welcome to LabourLink updates.'); }; return <section className="newsletter-section"><div className="section-inner newsletter-inner"><h2>Stay in the Loop</h2><p>Get job alerts, platform updates, and worker success tips delivered to your inbox.</p><form className="newsletter-form" onSubmit={subscribe}><label className="sr-only" htmlFor="newsletter-email">Email address</label><input id="newsletter-email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="Enter your email address" /><button>Subscribe</button></form></div></section>; }
